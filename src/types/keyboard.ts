export enum AppReturnKeyType {
  Default = "default",
  Next = "next",
  Done = "done",
  Go = "go",
  Search = "search",
  Send = "send",
}

export enum AppKeyboardType {
  Default = "default",
  EmailAddress = "email-address",
  Numeric = "numeric",
  PhonePad = "phone-pad",
  NumberPad = "number-pad",
  DecimalPad = "decimal-pad",
  ASCIICapable = "ascii-capable",
}